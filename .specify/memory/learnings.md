# voiceFaithfulness — Learnings

Soft patterns from dreaming (auto-applied unless `dry_run`). These are **not**
constitution.

Agents SHOULD follow these when they do not conflict with `constitution.md`.
Prefer `/speckit.dream.propose` (auto-applies) over editing this file by hand.
