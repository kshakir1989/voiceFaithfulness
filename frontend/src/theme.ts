/** Design tokens — keep in sync with CSS vars in `src/index.css` (shadcn). */
export const theme = {
  color: {
    primary: "#0F6B5C",
    background: "#E8EEF2",
    surface: "#F7FAFC",
    text: "#1A242E",
    textMuted: "#5A6A7A",
    danger: "#9B2C2C",
    line: "#C5D0DA",
  },
  space: 8,
} as const;
